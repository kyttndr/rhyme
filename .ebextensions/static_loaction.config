files:
  "/etc/nginx/conf.d/01_static_location.conf" :
    mode: "000644"
    owner: root
    group: root
    content: |
      location /static/ {
        alias /opt/rhyme/static/;
        autoindex on;
      }

  "/etc/nginx/myfile2" :
    mode: "000644"
    owner: root
    group: root
    content: |
      this is my
      file content

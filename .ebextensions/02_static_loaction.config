files:
  "/etc/nginx/conf.d/01_static_location.conf":
    mode: “000644”
    owner: root
    group: root
    content: |
      location /static/ {
        root /opt/rhyme/static/;
        autoindex off;
      }

container_commands:
  10_nginx_reload:
     command: "sudo service nginx reload"
